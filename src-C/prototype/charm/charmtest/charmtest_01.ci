mainmodule charmtest_01
{
  readonly int numElements;
  readonly CProxy_Main mainProxy;

  mainchare Main
  {
    entry Main (CkArgMsg *msg);
    entry void done ();
  };

  array [1D] Hello
  {
    entry Hello();
    entry void sayHi (int);
  };
};
