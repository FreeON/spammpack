module matrixnode
{
  extern module messages;
  chare MatrixNode
  {
    entry MatrixNode (int NLower[2], int NUpper[2], int chunksize);
    entry [sync] NodeInfoMsg * getInfo ();
    entry [sync] BlockMsg * getBlock ();
    entry [threaded] void get (int i, int j, CkFuture f);
    entry [threaded] void set (int i, int j, float aij, CkFuture f);
    entry [threaded] void multiply (CProxy_MatrixNode A, CProxy_MatrixNode B, CkFuture f);
  };
};
