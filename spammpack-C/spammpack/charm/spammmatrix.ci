module spammmatrix
{
  extern module messages;
  extern module matrixnode;
  chare SpAMMMatrix
  {
    entry SpAMMMatrix (const int N, const int chunksize);
    entry void get (const int i, const int j, float &aij);
    entry [threaded] void set (const int i, const int j, const float aij);
    entry void print ();
  };
};
