module matrix
{
  extern module messages;
  extern module matrixnode;
  chare Matrix
  {
    entry Matrix (int N, int chunksize);
    entry [sync] GetMsg* get (int i, int j);
    entry [sync] EmptyMsg* set (int i, int j, float aij);
    entry [sync] EmptyMsg* print ();
  };
};
