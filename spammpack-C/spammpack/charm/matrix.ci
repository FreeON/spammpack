module matrix
{
  extern module messages;
  extern module matrixnode;
  chare Matrix
  {
    entry Matrix ();
    entry Matrix (int N, int chunksize);
    entry [sync] EmptyMsg * initialize (int N, int chunksize);
    entry [sync] EmptyMsg * remove();
    entry [sync] IntMsg * getN ();
    entry [sync] IntMsg * getChunksize ();
    entry [sync] FloatMsg * get (int i, int j);
    entry [sync] EmptyMsg * set (int i, int j, float aij);
    entry [sync] EmptyMsg * print ();
  };
};
