mainmodule Futures
{
  message IntMsg;
  mainchare Futures
  {
    entry Futures (CkArgMsg *msg);
    entry [threaded] void run (int depth);
  };
  chare Node
  {
    entry Node ();
    entry [threaded] void compute (int tier, unsigned int index, int depth, CkFuture f);
  };
};
