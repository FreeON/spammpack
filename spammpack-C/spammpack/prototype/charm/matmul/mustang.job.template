#!/bin/bash -l
#
#MSUB -l nodes=1:ppn=1
#MSUB -N matmul-1

module purge
module load gcc/4.7.2
module load openmpi/1.6.3

#env | sort

cd $SLURM_SUBMIT_DIR

./charmrun -np ${SLURM_NTASKS} matmul -N 2048 -b 64
