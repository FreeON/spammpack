module node
{
  extern module messages;
  array [2D] Node
  {
    entry Node (int N, int depth, int blocksize, int tier, unsigned int seed);
    entry [sync] NodeInfoMsg * info (void);
    entry [sync] DenseMatrixMsg * getBlock (void);
    entry void set (int blocksize, double A[blocksize*blocksize]);
    entry void add (int blocksize, double A[blocksize*blocksize]);
  };
};
