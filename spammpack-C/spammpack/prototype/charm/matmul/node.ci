module node
{
  extern module messages;
  chare Node
  {
    entry Node (int depth, int blocksize, int tier,
        int iLower, int iUpper, int jLower, int jUpper);
    entry [sync] DoubleMsg * get (int i, int j);
    entry void initialize (int initType, int index, CkCallback &cb);
    entry void initializeDone (IntMsg *index);
    entry void matmul (int index, CProxy_Node A, CProxy_Node B, CkCallback &cb);
  };
};
