module node
{
  extern module messages;
  array [2D] Node
  {
    entry Node (int N, int depth, int blocksize, int tier, int iLower,
        int iUpper, int jLower, int jUpper);
    entry [sync] NodeBlockMsg * getBlock ();
    entry [sync] DoubleMsg * get (int i, int j);
    entry void initialize (int initType, CkCallback &cb);
    entry void printLeafPes (int index, CkCallback &cb);
  };
};
