module multiply
{
  extern module matrix;
  chare Multiply
  {
    entry Multiply ();
    entry [threaded] void multiply (CProxy_Matrix A, CProxy_Matrix B,
        CProxy_Matrix C, CkCallback &cb);
    entry [reductiontarget] void multiplyDone ();
    entry [reductiontarget] void storeBackDone ();
  };
};
