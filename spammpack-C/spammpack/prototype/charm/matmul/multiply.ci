#include "config.h"
module multiply
{
  extern module matrix;
  extern module multiplyelement;
  chare Multiply
  {
    entry Multiply ();
    entry void multiply (double tolerance, CProxy_Matrix A, CProxy_Matrix B,
        CProxy_Matrix C, CkCallback &cb);
#ifdef USE_REDUCTION_TARGET
    entry [reductiontarget] void multiplyDone ();
    entry [reductiontarget] void storeBackDone ();
#endif
  };
};
