module multiply
{
  extern module matrix;
  extern module node;
  array [3D] MultiplyElement
  {
    entry MultiplyElement (int blocksize,
        CProxyElement_Node ANode,
        CProxyElement_Node BNode,
        CProxyElement_Node CNode);
    entry void multiply (CkCallback &done);
  };
  chare Multiply
  {
    entry Multiply (CProxy_Matrix A, CProxy_Matrix B, CProxy_Matrix C);
    entry void multiply (CkCallback &cb);
    entry [reductiontarget] void multiplyDone ();
  };
};
