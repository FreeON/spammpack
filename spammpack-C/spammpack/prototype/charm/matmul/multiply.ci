module multiply
{
  extern module matrix;
  extern module multiplyelement;
  chare Multiply
  {
    entry Multiply ();
    entry [threaded] void multiply (double tolerance, CProxy_Matrix A,
        CProxy_Matrix B, CProxy_Matrix C, CkCallback &cb);
    entry void getComplexity (CkCallback &cb);
    entry [reductiontarget] void getComplexityDone (int complexity);
  };
};
