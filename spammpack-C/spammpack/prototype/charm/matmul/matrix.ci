module matrix
{
  extern module messages;
  extern module node;
  chare Matrix
  {
    entry Matrix (int N, int blocksize);
    entry [sync] DenseMatrixMsg * getDense ();
    entry [sync] MatrixInfoMsg * info (int tier);
    entry void random (CkCallback &cb);
    entry void zero (CkCallback &cb);
    entry void decay (CkCallback &cb);
    entry void initialize (int initType, CkCallback &cb);
    entry void print (CkCallback &cb);
    entry void printLeafPes (CkCallback &cb);
  };
};
