#include "spamm.h"

int
main (int argc, char *argv[])
{
  int result = 0;
  const int M = 103;
  const int N = 129;

  struct spamm_matrix_t *A;

  A = spamm_new(M, N);

  /* Load matrix from file. */

  return result;
}
